<%- include('../layout/table-head'); %>
<% if(data.length > 0) { %>
<table id="datatable-responsive" class="table table-striped table-bordered dt-responsive nowrap" cellspacing="0" width="100%">
        <thead>
        <tr>
            <th>#</th>
            <th>عنوان بخش</th>
            <th>عنوان دسترسی</th>
            <th>مسیر دسترسی</th>
            <th>توضیحات</th>
            <th></th>
        </tr>

        </thead>
        <tbody>
        <% data.forEach(item =>{ %>
            <tr>
                <th scope="row"><%= item.id %></th>
                <td><%= item.section_title %></td>
                <td><%= item.title %></td>
                <td><%= item.route_url %></td>
                <td><%= item.description %></td>
                <td>
                    <a href="/admin/permissions/<%= item.id %>/edit" class="btn btn-sm btn-warning">ویرایش</a>
                    <a href="/admin/permissions/<%= item.id %>/delete" class="btn btn-sm btn-danger">حذف</a>
                </td>
            </tr>
        <% }); %>
        </tbody>
    </table>

<% } else { %>
    <h1>کاربری یافت نشد.</h1>
<% } %>
<%- include('../layout/table-foot'); %>

