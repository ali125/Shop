<div class="x_panel">
    <div class="x_title">
        <h2>آپلود عکس</h2>
        <ul class="nav navbar-right panel_toolbox">
            <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            </li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    <a class="dropdown-item" href="#">Settings 1</a>
                    <a class="dropdown-item" href="#">Settings 2</a>
                </div>
            </li>
            <li><a class="close-link"><i class="fa fa-close"></i></a>
            </li>
        </ul>
        <div class="clearfix"></div>
    </div>
    <div class="x_content">
        <p>برای افزودن عکس بروی کادر زیر کلیک نمایید.</p>
        <div class="file-uploader" data-id="<%= upload_id %>">
            <% if(typeof data !== "undefined" && data) { %>
                <div class="upload-images-list">
                    <% data.forEach(item => { %>
                        <div class="image-file-upload-item">
                            <img src="<%= item.media_url %>" alt="<%= item.filename %>" />
                        </div>
                    <% }) %>
                </div>
            <% } %>
            <div class="file-input">
                <label for="file<%= upload_id %>"></label>
                <input type="file" id="file<%= upload_id %>" multiple />
            </div>
        </div>
    </div>
</div>
